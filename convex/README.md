# Convex Backend

Run locally with:

```bash
npx convex dev
```

Set deployment URL in `.env.local`:

```bash
NEXT_PUBLIC_CONVEX_URL="https://basic-seal-476.eu-west-1.convex.cloud"
NEXT_PUBLIC_CONVEX_ANON_KEY="PASTE_CONVEX_KEY_HERE"
```
